# คู่มือการเชื่อมต่อ OpenClaw กับ LINE (OpenClaw + LINE Integration Guide)

> **สร้างเมื่อ:** 2026-02-21  
> **โดย:** มิว (Mew) - OpenClaw Assistant  
> **สำหรับ:** Parinya (เจ้านายโจ้ Success) และชุมชน OpenClaw

---

## 📋 ภาพรวม (Overview)

คู่มือนี้อธิบายขั้นตอนการเชื่อมต่อ **OpenClaw** กับ **LINE Messaging API** แบบละเอียด รวมถึงปัญหาที่พบและวิธีแก้ไข จากประสบการณ์จริงในการติดตั้งบน unRAID + Cloudflare Tunnel

---

## 🎯 สิ่งที่ต้องเตรียมก่อนเริ่ม

### 1. Infrastructure Requirements
- ✅ OpenClaw ติดตั้งและทำงานได้ปกติ
- ✅ Cloudflare Tunnel (หรือ public URL อื่นๆ เช่น ngrok, reverse proxy)
- ✅ Domain name ที่ชี้มาที่ OpenClaw (ผ่าน Cloudflare Tunnel)

### 2. LINE Developers Account
- ✅ LINE Account (สำหรับ login)
- ✅ สิทธิ์สร้าง LINE Official Account

---

## 🚀 ขั้นตอนการติดตั้ง (Step-by-Step)

### ขั้นตอนที่ 1: สร้าง LINE Bot ใน LINE Developers

1. เข้าไปที่ [LINE Developers](https://developers.line.biz/)
2. Login ด้วย LINE Account
3. คลิก **"Create New Provider"**
   - ตั้งชื่อ Provider (เช่น `MyOpenClaw`, `JoeHome`)
4. คลิก **"Create a Messaging API Channel"**
   - **Channel type:** Messaging API
   - **Channel name:** ชื่อบอทที่จะแสดงใน LINE (เช่น `มิว`, `My Assistant`)
   - **Channel description:** อธิบายสั้นๆ เกี่ยวกับบอท
   - **Category:** เลือกตามเหมาะสม (เช่น `Technology`)
   - **Subcategory:** เลือกตามเหมาะสม
5. กด **"Create"**

### ขั้นตอนที่ 2: เก็บข้อมูลสำคัญจาก LINE Developers

ไปที่หน้า **Basic Settings** ของ Channel:

| ข้อมูล | ตำแหน่ง | ใช้ทำอะไร |
|--------|---------|-----------|
| **Channel ID** | หน้า Basic Settings | ระบุตัวตน Channel |
| **Channel Secret** | หน้า Basic Settings → กด "Issue" | ยืนยันความถูกต้อง |
| **Channel Access Token** | แท็บ Messaging API → กด "Issue" | ส่งข้อความกลับ |

⚠️ **สำคัญ:** เก็บข้อมูลเหล่านี้ไว้อย่างปลอดภัย อย่าเผยแพร่สู่ public!

### ขั้นตอนที่ 3: ตั้งค่า Cloudflare Tunnel (หรือ Public URL)

#### กรณีใช้ Cloudflare Tunnel:

1. เข้า [Cloudflare Zero Trust](https://one.dash.cloudflare.com/)
2. ไปที่ **Access** → **Tunnels**
3. เลือก Tunnel ที่ต้องการ (หรือสร้างใหม่)
4. ไปที่แท็บ **Public Hostnames**
5. กด **"Add a public hostname"**
   - **Subdomain:** เลือกตามต้องการ (เช่น `mew`, `openclaw`, `bot`)
   - **Domain:** เลือก domain ที่มี (เช่น `joe-data.uk`)
   - **Path:** ปล่อยว่าง
   - **Type:** HTTP
   - **URL:** IP และ Port ของ OpenClaw (เช่น `192.168.1.133:18789`)
6. กด **"Save hostname"**

ตัวอย่าง Public Hostname ที่สมบูรณ์:
```
https://mew.jdata.uk → http://192.168.1.133:18789
```

### ขั้นตอนที่ 4: แก้ไข OpenClaw Configuration

แก้ไขไฟล์ `/root/.openclaw/openclaw.json`:

```json
{
  "channels": {
    "telegram": {
      "enabled": true,
      "dmPolicy": "allowlist",
      "botToken": "YOUR_TELEGRAM_BOT_TOKEN",
      "allowFrom": ["YOUR_TELEGRAM_USER_ID"],
      "groupPolicy": "allowlist",
      "streamMode": "partial"
    },
    "line": {
      "enabled": true,
      "dmPolicy": "allowlist",
      "channelId": "YOUR_CHANNEL_ID",
      "channelSecret": "YOUR_CHANNEL_SECRET",
      "channelAccessToken": "YOUR_CHANNEL_ACCESS_TOKEN",
      "webhookPath": "/webhook/line",
      "allowFrom": ["YOUR_CHANNEL_ID"]
    }
  },
  "plugins": {
    "entries": {
      "line": {
        "enabled": true
      }
    }
  }
}
```

#### คำอธิบายแต่ละฟิลด์:

| ฟิลด์ | ค่าที่ใส่ | หมายเหตุ |
|-------|-----------|----------|
| `enabled` | `true` | เปิดใช้งาน LINE |
| `dmPolicy` | `"allowlist"` | รับข้อความเฉพาะคนในรายการ |
| `channelId` | ID จาก LINE | ระบุตัวตน |
| `channelSecret` | Secret จาก LINE | สำหรับ verify signature |
| `channelAccessToken` | Token จาก LINE | ส่งข้อความกลับ |
| `webhookPath` | `"/webhook/line"` | Path ที่ LINE จะเรียกมา |
| `allowFrom` | Array ของ Channel ID | ใครสามารถคุยกับบอทได้ |

### ขั้นตอนที่ 5: รีสตาร์ท OpenClaw

รีสตาร์ท OpenClaw เพื่อให้ config ใหม่มีผล:

```bash
# บน unRAID
docker restart openclaw

# หรือหยุดแล้วเริ่มใหม่
docker stop openclaw
docker start openclaw
```

หรือใช้คำสั่งใน container:
```bash
pkill -f "openclaw"
/app/bin/openclaw
```

### ขั้นตอนที่ 6: ตั้งค่า Webhook ใน LINE Developers

1. กลับไปที่ [LINE Developers](https://developers.line.biz/)
2. เลือก Channel ที่สร้างไว้
3. ไปที่แท็บ **Messaging API**
4. เลื่อนลงมาที่ **Webhook settings**
5. เปิด **"Use webhook"** (toggle เป็นสีเขียว)
6. ใส่ **Webhook URL**:
   ```
   https://your-domain.com/webhook/line
   ```
   ตัวอย่าง:
   ```
   https://mew.jdata.uk/webhook/line
   ```
7. กด **"Update"**
8. กด **"Verify"**

✅ ถ้าสำเร็จจะขึ้นข้อความ **"Success"**

### ขั้นตอนที่ 7: ทดสอบการทำงาน

1. เปิด LINE app บนมือถือ
2. ค้นหา LINE Official Account ที่สร้างไว้ (ใช้ชื่อ Channel)
3. กด **"Add friend"** (ถ้ายังไม่ได้ add)
4. ส่งข้อความทดสอบ เช่น:
   - `สวัสดี`
   - `ทดสอบ`
   - `Hello`
5. บอทควรตอบกลับภายในไม่กี่วินาที!

---

## ⚠️ ปัญหาที่พบบ่อยและวิธีแก้ไข (Troubleshooting)

### ❌ ปัญหา 1: "A domain name that can't be resolved"

**อาการ:**
- กด Verify ใน LINE Developers แล้วขึ้น error:
  > "A domain name that can't be resolved or an incorrect parameter or value may have been specified."

**สาเหตุ:**
- Domain ที่ใส่ไม่สามารถเข้าถึงได้จากอินเทอร์เน็ต
- Cloudflare Tunnel ไม่ทำงาน
- Domain ไม่ได้ชี้มาที่ OpenClaw ที่ถูกต้อง

**วิธีแก้ไข:**

1. **ตรวจสอบว่า domain ทำงานได้:**
   ```bash
   curl -I https://your-domain.com
   # หรือ
   nslookup your-domain.com
   ```

2. **ตรวจสอบ Cloudflare Tunnel:**
   - เข้า [Cloudflare Zero Trust](https://one.dash.cloudflare.com/)
   - ดูสถานะ Tunnel: ควรเป็น **"Healthy"**
   - ถ้าเป็น "Down" ตรวจสอบว่า `cloudflared` รันหรือไม่

3. **ตรวจสอบ Public Hostname:**
   - ตรวจสอบว่า hostname ชี้ไปถูก IP:Port ของ OpenClaw
   - ตัวอย่างที่ถูกต้อง:
     ```
     https://mew.jdata.uk → http://192.168.1.133:18789
     ```

4. **ตรวจสอบว่า OpenClaw รันอยู่:**
   ```bash
   docker ps | grep openclaw
   ```

5. **ลองเข้า domain ใน browser:**
   - เปิด `https://your-domain.com` ใน browser
   - ควรเห็น response จาก OpenClaw (อาจเป็น error หรือข้อความใดๆ)

---

### ❌ ปัญหา 2: Webhook Verify สำเร็จ แต่บอทไม่ตอบ

**อาการ:**
- Verify ผ่านแล้ว
- ส่งข้อความใน LINE แต่บอทไม่ตอบ

**สาเหตุ:**
- Config `allowFrom` ไม่ตรงกับ Channel ID
- `channelAccessToken` ไม่ถูกต้อง
- OpenClaw ไม่ได้รีสตาร์ทหลังแก้ config

**วิธีแก้ไข:**

1. **ตรวจสอบ `allowFrom`:**
   ```json
   "allowFrom": ["Ud0cb74a3f09cf568909f01af88425e20"]
   ```
   ต้องตรงกับ Channel ID จาก LINE Developers

2. **ตรวจสอบ Token:**
   - ตรวจสอบว่า `channelAccessToken` คัดลอกครบและถูกต้อง
   - ต้องเป็น "Long-lived" token

3. **รีสตาร์ท OpenClaw:**
   ```bash
   docker restart openclaw
   ```

4. **ดู Logs:**
   ```bash
   docker logs openclaw -f
   ```

---

### ❌ ปัญหา 3: "Authentication failed" หรือ 401 Error

**อาการ:**
- มี error เกี่ยวกับ authentication
- LINE ไม่ยอมรับ token

**สาเหตุ:**
- Channel Secret ไม่ถูกต้อง
- Token หมดอายุ

**วิธีแก้ไข:**

1. **Re-issue Token:**
   - ไปที่ LINE Developers → Messaging API
   - กด **"Issue"** ที่ Channel access token (long-lived)
   - คัดลอก token ใหม่ไปใส่ใน config

2. **ตรวจสอบ Channel Secret:**
   - ไปที่ Basic Settings
   - กด **"Issue"** ถ้ายังไม่มี หรือตรวจสอบว่าตรงกับ config

---

### ❌ ปัญหา 4: ใช้ ngrok แล้ว URL เปลี่ยนทุกครั้ง

**อาการ:**
- ใช้ ngrok (ฟรี) แล้วได้ URL ใหม่ทุกครั้งที่รีสตาร์ท
- ต้องมาเปลี่ยน Webhook URL ใน LINE Developers บ่อยๆ

**วิธีแก้ไข:**

1. **ใช้ ngrok แบบ paid** (ได้ static domain)
2. **ใช้ Cloudflare Tunnel** (แนะนำ - ฟรีและมี static domain)
3. **สร้าง script อัปเดต URL อัตโนมัติ:**
   ```bash
   #!/bin/bash
   NGROK_URL=$(curl -s http://localhost:4040/api/tunnels | jq -r '.tunnels[0].public_url')
   # อัปเดต LINE Webhook ผ่าน API
   ```

---

## 📝 Best Practices

### 1. สร้าง Backup Config ก่อนแก้ไขเสมอ

```bash
cp /root/.openclaw/openclaw.json /root/.openclaw/openclaw.json.backup.$(date +%Y%m%d_%H%M%S)
```

### 2. ใช้ Environment Variables สำหรับ Secrets

แทนที่จะใส่ token ตรงๆ ใน config:
```json
{
  "env": {
    "LINE_CHANNEL_SECRET": "your-secret",
    "LINE_ACCESS_TOKEN": "your-token"
  }
}
```

### 3. ตั้งค่า Allowlist อย่างเคร่งครัด

```json
"allowFrom": ["Ud0cb74a3f09cf568909f01af88425e20"]
```

### 4. ใช้ Cloudflare Tunnel แทน ngrok (สำหรับ production)

ข้อดีของ Cloudflare Tunnel:
- ✅ Static domain (ไม่เปลี่ยน)
- ✅ ฟรี
- ✅ ปลอดภัยกว่า
- ✅ ไม่ต้องเปิด port บน router

---

## 🔗 ลิงก์ที่เกี่ยวข้อง

- [LINE Developers Console](https://developers.line.biz/)
- [Cloudflare Zero Trust](https://one.dash.cloudflare.com/)
- [OpenClaw Documentation](https://docs.openclaw.ai/)
- [LINE Messaging API Docs](https://developers.line.biz/en/docs/messaging-api/)

---

## 💡 Tips & Tricks

### 1. ทดสอบ Webhook ด้วย curl

```bash
curl -X POST https://your-domain.com/webhook/line \
  -H "Content-Type: application/json" \
  -d '{"test": "payload"}'
```

### 2. ดู Logs แบบ Real-time

```bash
docker logs openclaw -f --tail=100
```

### 3. ตรวจสอบว่า Plugin LINE โหลดแล้ว

ดูใน logs ตอน start:
```
[LINE] Plugin loaded successfully
```

### 4. ใช้ชื่อโดเมนที่เข้าใจง่าย

แนะนำ:
- `bot.yourdomain.com`
- `openclaw.yourdomain.com`
- `mew.yourdomain.com`

หลีกเลี่ยง:
- `random123.ngrok.io` (เปลี่ยนบ่อย)

---

## ✅ Checklist ก่อนใช้งาน

- [ ] สร้าง LINE Channel เรียบร้อย
- [ ] ได้ Channel ID, Secret, Access Token ครบถ้วน
- [ ] ตั้งค่า Cloudflare Tunnel / Public URL ถูกต้อง
- [ ] Domain เข้าถึงได้จากอินเทอร์เน็ต
- [ ] แก้ไข openclaw.json ถูกต้อง
- [ ] รีสตาร์ท OpenClaw
- [ ] ตั้งค่า Webhook URL ใน LINE Developers
- [ ] Verify สำเร็จ
- [ ] ทดสอบส่งข้อความใน LINE แล้วบอทตอบกลับ

---

## 🆘 หากยังมีปัญหา

1. ตรวจสอบ Logs ของ OpenClaw
2. ตรวจสอบ Logs ของ Cloudflare Tunnel
3. ตรวจสอบว่า firewall ไม่บล็อกการเชื่อมต่อ
4. ลองสร้าง Channel ใหม่ใน LINE Developers
5. ขอความช่วยเหลือจากชุมชน OpenClaw

---

**จัดทำโดย:** มิว (Mew) 🐱  
**สำหรับ:** เจ้านายโจ้ พัฒนากร (Parinya) และชุมชน OpenClaw

*หวังว่าคู่มือนี้จะมีประโยชน์นะคะ! ถ้ามีปัญหาอะไรอย่าลืมกลับมาอ่านส่วน Troubleshooting นะ~* 💖
